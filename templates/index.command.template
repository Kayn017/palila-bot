const { init, shutdown, execute, middleware } = require("./actions");
const { configure, configurations } = require("./config");
const { name, author, description, explication, options, intents, permissions } = require("./infos");

module.exports = {
	// actions
	init,
	shutdown,
	execute,
	middleware,

	// config
	configure,
	configurations,

	// infos
	name,
	author,
	description,
	explication,
	options,
	intents,
	permissions,
};